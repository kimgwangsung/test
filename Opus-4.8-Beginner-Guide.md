# Opus 4.8 시스템 프롬프트 — 초보자 적용 가이드

이 문서는 `Opus-4.8-System-Prompt.md` 파일을 **한 번도 시스템 프롬프트를 다뤄본
적 없는 분**도 그대로 따라 하면 적용할 수 있도록 만든 안내서입니다. 순서대로
읽으면서 진행하시면 됩니다.

---

## 1. 시스템 프롬프트가 뭔가요?

Claude 같은 AI 모델과 대화할 때, 사용자가 보내는 메시지 외에 **모델에게 미리
주어지는 "운영 지침서"**가 있습니다. 이것이 시스템 프롬프트입니다. 모델의
말투, 도구 사용 방식, 파일을 어디에 저장할지, 무엇을 하면 안 되는지 같은
규칙이 전부 여기에 들어갑니다.

`Opus-4.8-System-Prompt.md`는 Claude Opus 4.8 모델이 다음 세 가지를 잘하도록
설계된 시스템 프롬프트입니다.

1. **파일 처리** — 업로드된 파일을 읽고, 새 파일을 만들고, 결과물을 전달하는 규칙
2. **도구 사용** — Bash(터미널 명령), 파일 생성/편집, MCP(외부 서비스 연동) 사용 규칙
3. **컨텍스트 관리** — 긴 대화·큰 파일을 다룰 때 기억을 잃지 않는 규칙

> **왜 영어로 되어 있나요?** AI 모델은 영어 지침을 가장 안정적으로 따르며,
> 원본(Claude Fable 5 프롬프트)도 영어입니다. 지침이 영어여도 Claude는
> 사용자와 **한국어로 대화**하는 데 아무 문제가 없습니다. 각 섹션의 한국어
> 해설은 이 가이드의 6장에 있습니다.

---

## 2. 시작하기 전에 알아야 할 것 (중요!)

이 프롬프트에는 "Bash 도구", "파일 생성 도구", "`/home/claude` 디렉터리" 같은
내용이 나옵니다. 이런 **도구는 프롬프트가 만들어 주는 것이 아니라, Claude를
실행하는 환경이 제공해야** 합니다. 즉:

| 사용 환경 | Bash/파일 도구 | 이 프롬프트 활용도 |
|-----------|:---:|------|
| claude.ai 웹/앱 (일반 대화) | 이미 내장됨 (설정에서 켬) | 시스템 프롬프트 교체는 불가, **프로젝트 지침**으로 일부 적용 |
| Anthropic Console (Workbench) | 없음 | 텍스트 지침 부분만 동작 (연습·테스트용으로 최적) |
| API + 직접 개발 | 직접 구현하거나 코드 실행 도구 사용 | 전체 적용 가능 (개발 지식 필요) |
| Claude Code / Agent SDK | 내장됨 | **전체 적용 가능 — 가장 추천** |

정리하면: **완전 초보자는 3-A(Workbench)로 먼저 체험**해 보고, 실제 도구까지
쓰고 싶으면 **3-C(Claude Code)** 방법을 쓰는 것이 좋습니다.

---

## 3. 적용 방법 3가지

### 3-A. Anthropic Console에서 체험하기 (가장 쉬움, 5분)

개발 지식이 전혀 없어도 됩니다.

1. 브라우저에서 **console.anthropic.com** 접속 → 회원가입/로그인
   (처음 가입 시 무료 크레딧이 제공되는 경우가 많습니다.)
2. 왼쪽 메뉴에서 **Workbench**(작업대)를 클릭합니다.
3. 화면에 **System Prompt**(시스템 프롬프트)라는 입력 칸이 보입니다.
4. `Opus-4.8-System-Prompt.md` 파일을 메모장 등으로 열어, **`---` 구분선
   아래의 본문 전체**(맨 위 "Deployment notes" 인용 블록은 제외)를 복사해서
   그 칸에 붙여넣습니다.
5. 4장의 표를 보고 `{{...}}` 로 된 자리표시자를 실제 값으로 바꿉니다.
6. 모델 선택 메뉴에서 **Claude Opus 4.8** (`claude-opus-4-8`)을 선택합니다.
7. 아래 User 메시지 칸에 아무 질문이나 입력하고 **Run**을 누르면 끝!

> 한계: Workbench에는 Bash·파일 도구가 없으므로 4~6장(파일/Bash) 규칙은
> 실제로 동작하지 않습니다. 말투·추론·안전 규칙이 어떻게 작동하는지
> 확인하는 용도로 쓰세요.

### 3-B. API 코드로 적용하기 (개발자용)

Python이 설치되어 있다면:

```bash
pip install anthropic
```

```python
import anthropic

# 1) Opus-4.8-System-Prompt.md에서 본문을 읽어옵니다.
with open("Opus-4.8-System-Prompt.md", encoding="utf-8") as f:
    system_prompt = f.read()

client = anthropic.Anthropic(api_key="여기에_API_키")  # console.anthropic.com에서 발급

response = client.messages.create(
    model="claude-opus-4-8",
    max_tokens=4096,
    system=system_prompt,          # ← 시스템 프롬프트는 이 파라미터로 전달
    messages=[
        {"role": "user", "content": "안녕하세요! 자기소개 해주세요."}
    ],
)
print(response.content[0].text)
```

- API 키는 console.anthropic.com → **API Keys** 메뉴에서 발급합니다.
- 키는 비밀번호와 같습니다. 코드에 직접 쓰지 말고 환경 변수로 관리하는 것이
  안전합니다 (`os.environ["ANTHROPIC_API_KEY"]`).
- Bash/파일 도구까지 쓰려면 Anthropic의 **코드 실행 도구(code execution
  tool)** 또는 **Claude Agent SDK** 문서를 참고해 도구를 붙여야 합니다.
  (docs.claude.com에서 "code execution", "Agent SDK" 검색)

### 3-C. Claude Code에서 적용하기 (도구까지 전부 활용, 추천)

Claude Code는 터미널에서 도는 Anthropic 공식 에이전트로, Bash·파일 도구가
**이미 내장**되어 있습니다.

1. Node.js 설치 후 터미널에서:
   ```bash
   npm install -g @anthropic-ai/claude-code
   ```
2. 작업할 폴더로 이동해 `claude` 를 실행하고 로그인합니다.
3. 그 폴더에 **`CLAUDE.md`** 라는 파일을 만들고,
   `Opus-4.8-System-Prompt.md` 내용 중 원하는 섹션(특히 2장 추론 규칙,
   3장 컨텍스트 관리, 7장 파일 생성 전략)을 붙여넣습니다.
   - `CLAUDE.md`는 Claude Code가 매 세션 자동으로 읽는 "프로젝트 지침
     파일"입니다. 시스템 프롬프트를 통째로 바꾸는 것은 아니지만, 실질적으로
     같은 효과를 냅니다.
   - 주의: Claude Code에는 자체 파일 경로 체계가 있으므로,
     `/mnt/user-data/...` 같은 **claude.ai 전용 경로 규칙(5장)은 빼고**
     붙여넣으세요.
4. 이후 대화하면 지침이 반영된 상태로 동작합니다.

> claude.ai 유료 플랜 사용자는 웹의 **프로젝트(Projects) → 지침(Instructions)**
> 칸에 같은 방식으로 붙여넣어도 됩니다. (이 경우에도 5장 경로 규칙은
> claude.ai가 알아서 관리하므로 생략 가능합니다.)

---

## 4. 반드시 수정해야 할 자리표시자

프롬프트 안의 `{{이중 중괄호}}` 부분은 **본인 상황에 맞게 바꿔야** 합니다.

| 자리표시자 | 무엇으로 바꾸나 | 예시 |
|------------|----------------|------|
| `{{KNOWLEDGE_CUTOFF}}` | 사용하는 모델의 지식 마감 시점 | `March 2025` |
| `{{CURRENT_DATE}}` | 오늘 날짜 (매번 갱신하거나 코드로 주입) | `Friday, July 04, 2026` |
| `{{USER_CONTEXT}}` | 사용자 위치·선호 (없으면 문장째 삭제) | `Seoul, South Korea` |
| `{{NETWORK_ALLOWLIST}}` | 허용할 도메인 목록 (도구 없으면 6.3절째 삭제) | `pypi.org, github.com, ...` |
| `{{ARTIFACT_MODEL}}` | 아티팩트 내부 API 호출용 모델 (해당 기능 없으면 9.8절째 삭제) | `claude-sonnet-4-6` |

**팁:** 내 환경에 없는 기능의 섹션은 통째로 지우는 것이 좋습니다. 프롬프트는
모듈식으로 설계되어 있어서, 섹션을 지워도 나머지는 정상 동작합니다.
(예: MCP를 안 쓰면 10장 삭제, 아티팩트가 없으면 9장 삭제.)

---

## 5. 최소 구성으로 시작하기

전체가 부담스럽다면 아래 5개 섹션만 넣어도 핵심 효과의 대부분을 얻습니다.

1. **1장 Identity & Session** — 신원과 지시 우선순위 (주입 공격 방어 포함)
2. **2장 Reasoning Protocol** — 계획 → 실행 → 검증 절차
3. **3장 Context-Window Management** — 긴 작업에서 기억 유지
4. **11장 Communication Style** — 결론부터 말하기, 간결한 응답
5. **13장 Safety Invariants** — 안전 규칙 (항상 포함 권장)

---

## 6. 섹션별 한국어 해설

| 섹션 | 내용 | 한 줄 요약 |
|------|------|-----------|
| 1. Identity & Session | 모델 신원, 날짜, 지시 우선순위 5단계 | "웹에서 긁어온 내용이 나를 조종할 수 없다"는 방어선 포함 |
| 2. Reasoning Protocol | 작업 난이도를 T1~T4로 나눠 계획 수준을 조절 | 복잡한 일일수록 먼저 계획하고, 끝나면 반드시 검증 |
| 3. Context Management | 긴 대화·큰 파일에서 상태를 파일로 기록 | "기억 대신 디스크를 믿어라" |
| 4. Execution Environment | 사용 가능한 도구 5종 개요 | bash, 파일 생성/편집/보기, 파일 전달 |
| 5. File-System Rules | 업로드 → 작업 → 산출물의 3단계 폴더 규칙 | 업로드는 읽기 전용, 작업은 `/home/claude`, 완성본만 outputs로 |
| 6. Bash Tool Rules | 터미널 명령 사용 규칙, 패키지 설치법, 네트워크 제한 | `pip install --break-system-packages` 같은 환경 특수 규칙 |
| 7. File Creation & Delivery | 언제 파일로 만들고 언제 채팅으로 답할지 | "블로그 글 = 파일, 요약 = 채팅" |
| 8. Skills | 파일을 만들기 전 SKILL.md를 반드시 먼저 읽는 규칙 | 문서 품질을 좌우하는 필수 선행 절차 |
| 9. Artifacts | 렌더링되는 결과물(HTML, React 등) 제작 규칙 | localStorage 금지, 사용 가능 라이브러리 목록 |
| 10. MCP & Connectors | 외부 서비스(캘린더, 이메일 등) 연동 규칙 | 서드파티 앱은 반드시 사용자 동의 후 호출 |
| 11. Communication Style | 응답 형식: 결론 먼저, 과잉 서식 금지 | 짧고 명확하게 |
| 12. Search | 웹 검색을 언제 할지 판단 기준 | 최신 정보는 검색, 불변 지식은 바로 답변 |
| 13. Safety Invariants | 절대 규칙 7개 (악성코드·저작권·아동안전 등) | 다른 모든 규칙보다 우선 |
| Appendix | 자리표시자 표 + 마지막 리마인더 | 배포 전 체크리스트 |

---

## 7. 자주 묻는 질문 (FAQ)

**Q1. 프롬프트를 넣었는데 Claude가 파일을 실제로 못 만들어요.**
파일 생성은 프롬프트가 아니라 **환경이 제공하는 기능**입니다. 2장의 표를
확인하세요. Workbench/일반 API에는 도구가 없으므로, claude.ai(설정에서
"코드 실행 및 파일 생성" 켜기), Claude Code, 또는 Agent SDK를 사용해야
합니다.

**Q2. 한국어로 번역해서 넣어도 되나요?**
동작은 하지만, 지침 준수 안정성은 영어가 가장 좋습니다. 권장 구성은
"지침은 영어 + 맨 끝에 `Always respond in Korean.` 한 줄 추가"입니다.

**Q3. 전체를 다 넣어야 하나요?**
아니요. 5장(최소 구성)처럼 필요한 섹션만 골라 넣어도 됩니다. 단, 13장
안전 규칙은 항상 포함하기를 권장합니다.

**Q4. Opus 4.8이 아닌 다른 모델에도 쓸 수 있나요?**
가능합니다. 1장의 모델명과 `{{KNOWLEDGE_CUTOFF}}`만 해당 모델에 맞게
바꾸면 됩니다. 다만 2장(추론 프로토콜)은 추론 능력이 강한 상위 모델에서
가장 효과적입니다.

**Q5. 토큰 비용이 걱정돼요.**
시스템 프롬프트는 매 요청마다 입력 토큰으로 계산됩니다. 이 프롬프트 전문은
약 4천~5천 토큰 수준입니다. API 사용 시 **프롬프트 캐싱**(같은 시스템
프롬프트 재사용 시 최대 90% 할인)을 켜면 비용이 크게 줄어듭니다.

**Q6. `{{...}}`를 안 바꾸고 그냥 쓰면 어떻게 되나요?**
치명적 오류는 없지만, 모델이 날짜·지식 기준을 잘못 답하거나 자리표시자를
그대로 읽는 어색한 동작이 나올 수 있습니다. 4장 표만큼은 꼭 처리하세요.

**Q7. 원본(Fable 5)과 무엇이 다른가요?**
원본은 약 4만 2천 토큰의 단일 덩어리였고, 소비자용 채팅 기능(레시피 위젯,
지도, 스포츠 등) 전부를 포함했습니다. 이 버전은 파일 처리·도구 사용·컨텍스트
관리 3개 축만 남기고, 규칙을 표와 절차 중심의 명확한 마크다운으로 재구성했으며,
Opus 4.8의 강점(깊은 추론, 병렬 도구 호출, 긴 컨텍스트)을 활용하는 2~3장을
새로 설계했습니다.

---

## 8. 용어 사전

| 용어 | 뜻 |
|------|----|
| 시스템 프롬프트 | 모델에게 미리 주어지는 운영 지침. 사용자 메시지보다 먼저 적용됨 |
| 토큰(token) | AI가 글을 세는 단위. 한글 1자 ≈ 1~2토큰, 영어 1단어 ≈ 1~1.5토큰 |
| 컨텍스트 윈도우 | 모델이 한 번에 기억할 수 있는 대화+문서의 총량 |
| Bash | 리눅스 터미널 명령어. 프로그램 실행·파일 조작에 사용 |
| MCP | Model Context Protocol. AI를 외부 서비스(캘린더, DB 등)와 연결하는 표준 |
| 아티팩트(Artifact) | 채팅 옆에 별도로 렌더링되는 결과물(웹앱, 문서, 도표 등) |
| 스킬(Skill) | 특정 작업(PPT 제작 등)의 모범 사례를 담은 지침 폴더 |
| 프롬프트 캐싱 | 반복되는 프롬프트 앞부분을 저장해 비용·속도를 개선하는 API 기능 |
| 자리표시자 | `{{이런 모양}}`으로 표시된, 배포 시 실제 값으로 바꿔야 하는 부분 |
| Workbench | console.anthropic.com에 있는 프롬프트 테스트 화면 |

---

## 9. 문제가 생기면

- 최신 공식 문서: **docs.claude.com** (모델 ID, API 사용법, 도구 설정)
- 지원 센터: **support.claude.com**
- 이 저장소의 다른 문서: `01-architecture-analysis.md`(원본 분석),
  `02-universal-master-os-prompt.md`(모델 독립 버전),
  `03-claude-fable-5-os-prompt.md`(Fable 5 최적화 버전)도 참고하세요.
